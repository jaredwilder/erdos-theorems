# P6 Erdős–Hajnal — recovered finite classification layer

**Author:** Jared Wilder  
**Recovered from:** release-day archive seam, 2026-09-11  
**Status:** finite classification records recovered from source archives; preserve separately from the unbounded theorem bank until their original verifier/source bytes are routed here.

The archive-seam audit found several exact finite classifications from the P6 campaign that were not visible from this program's human front door.

## Three-tooth complement-comb census

In the source-bound three-tooth complement-comb model with stable handles and two vertices per tooth, the recovered exhaustive census reports:

- total configurations: **32,768**;
- configurations containing an induced `P6`: **5,680**;
- configurations rejected by the pairwise rectangle law: **5,440**;
- pairwise-legal configurations that still contain a `P6`: **240**;
- inclusion-minimal forcing patterns among those survivors: **252**, falling into **8 symmetry orbits**.

This is useful precisely because it quantifies the gap between pairwise legality and true `P6`-freeness rather than merely saying the pairwise condition is insufficient.

## Private-representative `K(3,3)` census

In the induced dominating `K(3,3)` private-representative model, the source encodes the 15 private edges and exhausts all **32,768** states.

The recovered result is exceptionally rigid:

> **Exactly four states extend to a `P6`-free full graph, and all four are cluster graphs.**

The four survivors are recorded as the empty state, the `X`-clique state, the `Y`-clique state, and the complete state.

## P5-signature classification

The same archive seam records a **30-case classification of nonempty proper `P5` signatures**, with every rejected case carrying an explicit six-vertex co-`P6` witness in the source campaign.

## Authority boundary

These are not promoted here to an unbounded Erdős–Hajnal theorem. They are finite structural classifications recovered from the campaign's source archives.

The original exhaustive-enumeration source and verifier bytes were not present in this public directory at the time of promotion. Until those bytes are recovered and rerun, this file is the public mathematical index of the recovered classifications, while the existing theorem bank remains the authority for proof-bearing unbounded statements.

The classification layer is large and coherent enough that it must remain visible when this program moves to a dedicated P6 repository.
